# Windsurf Rules

Configuration for Windsurf AI IDE.

```json
{
  "rules": [
    "Follow layered architecture",
    "Ensure high cohesion and low coupling",
    "All new functions must have error handling",
    "No hardcoded secrets or parameters",
    "Verify changes with unit tests or logs"
  ]
}
```
